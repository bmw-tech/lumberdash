name: example
description: Example project for Lumberjack
version: 1.0.0

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  lumberdash:
    path: ../../../
  firebase_lumberdash:
    path: ../

dev_dependencies:

