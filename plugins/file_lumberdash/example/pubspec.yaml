name: example
description: Example project for file_lumberdash
version: 1.0.0+1

environment:
  sdk: ">=2.4.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^1.6.5
  lumberdash: ^2.1.0
  file_lumberdash:
    path: ../

dev_dependencies:
