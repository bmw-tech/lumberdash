name: example
description: Example project for Lumberjack
version: 1.0.0

environment:
  sdk: ">=2.4.0 <3.0.0"

dependencies:
  lumberdash:
    path: ../../../
  colorize_lumberdash:
    path: ../

dev_dependencies:
